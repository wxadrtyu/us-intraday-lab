# v10824 execution-causality revocation

`lev-v10824-dc64eea19fd64bd8` remains a faithfully reproduced research result,
but it is not admissible for Paper execution.  The independent forward
assembler matches the research return stream exactly; that parity exposed a
deeper timing violation in the research construction.

The modern and fallback anchor parents decide at bar 23.  On sessions where
one of those routes later proves inactive, the frozen wide-fill ensemble can
contain parents entered at bars 11, 12, or 18.  The backtest therefore fills
cash before the absence of the selected anchor is knowable.  This is a
look-ahead dependency even though each individual parent signal is causal.

The machine-readable parity supplement records the affected sessions,
component-session pairs, and return contribution.  Paper admission is revoked
as `REJECTED_NONCAUSAL_EARLY_FILL_BEFORE_ROUTE_RESOLUTION`.  Existing Paper
allocations are unchanged.
