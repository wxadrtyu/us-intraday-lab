# v1966-v2065 preregistration — 2026-08-29

Frozen before execution. Use codex/full-data-v13-2026-gated in the isolated
heartbeat-v4 worktree; leave the 9fe8 successor checkout and its runtime state alone.

## Version and comparison reservation

Reserve v1966-v2065: 100 versions, 2,650 parameter cells. The latest successor
report (v1865-v1965, commit 10abc22 ancestry) reports 111,005 cumulative cells.
New conservative global comparison count: 113,655. No v46-v1965 scan is rerun.
Output directory: E:/us-intraday-lab-data/research/v1966-v2065.
The G: volume is nearly full; existing source data and caches remain read-only.

## Mechanisms

v1966-v2015: 25 declared market-state factor combinations from the v349 registry
at two genuinely different causal structures. First, opening state minus the
preceding completed close state; second, prior close state minus its preceding
close. These test improving/deteriorating conditions rather than levels. Missing
inputs remain missing and force no signal. Frozen v45 is the observation-exception
anchor; enhancement classification does not imply full factory admission.

v2016-v2065: ten distinct exhaustion/recovery factor combinations (see frozen
source) over opening, late-morning, lunch, afternoon-reset and closing windows.
These independently select TQQQ/SOXL without using the v45 trade stream.
Threshold, confirmation, volatility-target and lookback cells are internal
parameters, never additional version numbers.

## Gates and limitations

Fit and rank using 2022-2025 only. 2024-2025 has been reused for development and is
not fresh untouched OOS. 2026 is consumed diagnostic, total return >5%, never a
ranking input. Standard 9bp, 18bp and extra five-minute delay must all pass original
primary gates (50% annualized, MDD <20%, IR >=1), alongside folds, start-date stress,
70% parameter-neighborhood support, independent 2018-2020 history and global
multiple-comparison pressure. Native factory null required for every pre-null
candidate. A failed hard gate never becomes an admission or an exception.

This is brokerless research only. No order, Paper state or existing observation
is modified. Reports must distinguish 100 versions, 2,650 cells and 300 frontier
records; overlapping rejection counts are not unique strategy counts.
